import { FilterDropdownsPipe } from './filter-dropdowns.pipe';

describe('FilterDropdownsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDropdownsPipe();
    expect(pipe).toBeTruthy();
  });
});
